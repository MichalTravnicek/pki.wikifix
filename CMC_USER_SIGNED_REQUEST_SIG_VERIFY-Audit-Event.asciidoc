= Overview =

The `CMC_USER_SIGNED_REQUEST_SIG_VERIFY` audit event is generated when CMC (user-signed or self-signed) certificate requests or revocation requests are submitted and signature is verified.

= Examples =
