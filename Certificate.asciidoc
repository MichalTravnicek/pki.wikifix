A public key certificate (or identity certificate) is an electronic text file which uses a digital signature from a Certificate Authority to bind a public key with an identity. The certificate has information such as the name of a person or an organization, an address and so forth. The certificate can be used to verify that a public key belongs to an individual or other entity.
