= Overview =

This page describes the process to generate a CMC shared token for link:Issuing-User-Certificate-with-CMC-Shared-Token[issuing a user certificate].

It assumes that:

* Issuance protection certificate has been link:Creating-Issuance-Protection-Certificate[created].

= Generating Shared Token =

To generate a shared token:

----
$ CMCSharedToken \
    -d /etc/pki/pki-tomcat/alias \
    -p Secret.123 \
    -n ca_issuance_protection \
    -s <token> \
    -o testuser.b64
----

The token will be wrapped with issuance protection's public key and stored into `testuser.b64`. Load the encrypted token and convert it into a single line with the following command:

----
$ SHARED_TOKEN=$(sed -e :a -e 'N;s/\n//;ba' testuser.b64)
----

= Adding Shared Token into User Record =

----
$ ldapmodify \
    -H ldap://ds.example.com:3389 \
    -x \
    -D "cn=Directory Manager" \
    -w Secret.123 \
    << EOF
dn: uid=testuser,ou=people,dc=example,dc=com
changetype: modify
add: objectClass
objectClass: extensibleobject
-
add: shrTok
shrTok: $SHARED_TOKEN
-
EOF
----

= See Also =

* link:PKI-10.5-CMC-Shared-Token[PKI 10.5 CMC Shared Token]
