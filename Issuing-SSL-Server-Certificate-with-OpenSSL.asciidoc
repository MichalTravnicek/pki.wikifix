= Issuing SSL Server Certificate with OpenSSL =

To issue an SSL Server Certificate with an existing CA certificate and key:

----
$ openssl x509 \
    -req \
    -CA ca.crt \
    -CAkey ca.key \
    -CAcreateserial \
    -in sslserver.csr \
    -out sslserver.crt
----

= See Also =

* link:Generating-SSL-Server-Certificate[Generating SSL Server Certificate]
* link:Generating-SSL-Server-CSR-with-OpenSSL[Generating SSL Server CSR with OpenSSL]
* link:Creating-Self-Signed-SSL-Server-Certificate-with-OpenSSL[Creating Self-Signed SSL Server Certificate with OpenSSL]
* link:https://www.dogtagpki.org/wiki/OpenSSL[OpenSSL]
