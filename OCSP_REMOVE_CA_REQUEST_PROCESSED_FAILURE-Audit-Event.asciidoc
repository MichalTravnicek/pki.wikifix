= Overview =

The `OCSP_REMOVE_CA_REQUEST_PROCESSED_FAILURE` audit event is generated when a remove CA request to the OCSP Responder is processed and failed.

*Note:* In PKI 10.5 the `OCSP_REMOVE_CA_REQUEST_PROCESSED_FAILURE` event is renamed into `OCSP_REMOVE_CA_REQUEST_PROCESSED` event.