= Overview

The Login operation will authenticate the user, establish a session on the server, return the user profile, and return a cookie containing the session ID.

= Request

* *URL:* `/acme/login`
* *Method:* `POST`
* *Auth required:* Yes

= Success Response

* *Code:* `200 OK`

= Error Response

* *Code:* `401 Unauthorized`
