= Overview =

The `pki-server restart` command can be used to restart PKI server that is running in the background. 

= Restarting PKI Server =

To restart PKI server execute the following command:

----
$ pki-server restart <name>
----
