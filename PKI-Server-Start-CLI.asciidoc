= Overview =

The `pki-server start` command can be used to start PKI server in the background. 

= Starting PKI Server =

To start PKI server execute the following command:

----
$ pki-server start tomcat@<name>
----
