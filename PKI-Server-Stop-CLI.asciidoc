= Overview =

The `pki-server stop` command can be used to stop PKI server that is running in the background. 

= Stopping PKI Server =

To stop PKI server execute the following command:

----
$ pki-server stop <name>
----
