= TPS Operation

* op=update_audit_admin

= REST Operation

* PATCH /tps/rest/audit

= Request

* Enable audit logging
* Enable audit log signing
* Audit log signing interval
* Audit log signing buffer size
* Events always logged
* Optional events to be logged
* ETag
* Nonce

= Response

* HTTP 200 OK
* Enable audit logging
* Enable audit log signing
* Audit log signing interval
* Audit log signing buffer size
* Events always logged
* Optional events to be logged
* ETag
