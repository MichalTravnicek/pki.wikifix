= TPSOperation

* op=select_config_parameter&ptype=Profile_Mappings

= REST Operation

* GET /tps/rest/profilemappings

= Request

* Page number (default: 1)
* Page size (default: 20)

= Response

* HTTP 200 OK
* Profile mappings in the requested page:
** Profile Mapping ID
