= Overview =

WARNING: This document is still under construction.

This document describes several procedures to renew PKI admin certificate.

= Using CMSGateway =

NOTE: This procedure may require using Firefox earlier than version 69 that still supports `<keygen>`.

To renew the admin certificate using CMSGateway, configure the following parameter in `/etc/pki/pki-tomcat/ca/CS.cfg`:

----
cmsgateway.enableAdminEnroll=true
----

Restart the server, then follow the procedure at https://localhost.localdomain:8443/ca/admin/ca/adminEnroll.html.

= Using pki-server cert-fix =

See link:Renewing-Admin-Certificate-using-pki-server-cert-fix[Renewing Admin Certificate using pki-server cert-fix].

= Using PKI NSS CLI =

See link:Renewing-Admin-Certificate-using-PKI-NSS-CLI[Renewing Admin Certificate using PKI NSS CLI].
